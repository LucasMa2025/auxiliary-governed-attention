# AGA - Auxiliary Governed Attention
# ===================================
# 产品级原型系统依赖 v3.4

# ==================== 核心依赖 ====================
torch>=2.0.0
transformers>=4.35.0
numpy>=1.24.0

# ==================== Web 接口（实验工具） ====================
flask>=2.3.0
flask-cors>=4.0.0
pyyaml>=6.0

# ==================== REST API（治理系统集成） ====================
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.5.0

# ==================== HTTP 客户端 ====================
httpx>=0.25.0        # For API client, Ollama adapter

# ==================== LLM 适配器 ====================
openai>=1.0.0        # For DeepSeek, vLLM adapters

# ==================== 持久化（必需） ====================
aiosqlite>=0.19.0    # SQLite 异步支持

# ==================== 压缩支持 ====================
lz4>=4.3.0           # LZ4 压缩算法
# zstd>=1.5.0        # Zstandard 压缩（可选，更高压缩率）

# ==================== 持久化（生产环境） ====================
# Redis - 热缓存层 (L1)
# redis>=5.0.0

# PostgreSQL - 冷存储层 (L2)
# asyncpg>=0.29.0

# ==================== 分布式（生产环境） ====================
# Kafka - 消息总线
# aiokafka>=0.10.0

# ==================== 监控（生产环境） ====================
# prometheus-client>=0.19.0
# opentelemetry-api>=1.20.0
# opentelemetry-sdk>=1.20.0
# opentelemetry-exporter-otlp>=1.20.0

# ==================== 大模型支持 ====================
# accelerate>=0.24.0
# bitsandbytes>=0.41.0

# ==================== vLLM 本地部署 ====================
# vllm>=0.2.0

# ==================== 评估工具 ====================
# lm-eval>=0.4.0
# datasets>=2.14.0

# ==================== 测试依赖 ====================
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0        # 覆盖率报告
pytest-xdist>=3.0.0      # 并行测试

# ==================== 开发工具 ====================
# black>=23.0.0
# isort>=5.0.0
# mypy>=1.0.0
